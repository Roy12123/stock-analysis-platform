name: 股東持有比例差 (每日 10:00 台北時間)

on:
  schedule:
    - cron: '0 2 * * *'  # UTC 02:00 = 台北 10:00
  workflow_dispatch:  # 允許手動觸發

permissions:
  contents: write  # 允許推送到 repository

jobs:
  run-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4

      - name: 設置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安裝 Python 依賴
        run: |
          cd python
          pip install -r requirements.txt

      - name: 執行股東持有比例差分析
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: |
          cd python
          python 股東持有比例差.py

      - name: 整理資料檔案
        run: |
          # 確保目錄存在
          mkdir -p data/latest
          mkdir -p data/history/$(date +%Y-%m-%d)

          # 移動最新資料到 latest 目錄
          find python -name "*大戶持有比例差.csv" -exec mv {} data/latest/大戶持有比例差.csv \; || true

          # 複製到歷史資料目錄
          cp data/latest/大戶持有比例差.csv data/history/$(date +%Y-%m-%d)/ || true

          # 顯示檔案清單
          echo "=== 最新資料 ==="
          ls -lh data/latest/
          echo "=== 歷史資料 ==="
          ls -lh data/history/$(date +%Y-%m-%d)/

      - name: 提交變更到 GitHub
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add data/
          git diff --staged --quiet || git commit -m "📈 更新股東持有比例差資料 - $(date +'%Y-%m-%d %H:%M') (台北時間: $(TZ='Asia/Taipei' date +'%Y-%m-%d %H:%M'))"
          git push || echo "沒有變更需要推送"
