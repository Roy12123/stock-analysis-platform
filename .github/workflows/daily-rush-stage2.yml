name: 隔日衝策略 - 階段2 (每日 15:00 台北時間)

on:
  schedule:
    - cron: '0 7 * * *'  # UTC 07:00 = 台北 15:00
  workflow_dispatch:  # 允許手動觸發

permissions:
  contents: write  # 允許推送到 repository

jobs:
  realtime-screen:
    runs-on: ubuntu-latest

    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4

      - name: 設置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安裝 Python 依賴
        run: |
          cd python
          pip install -r requirements.txt

      - name: 執行階段2 - 即時篩選
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: |
          cd python
          python 隔日衝策略_階段2.py

      - name: 整理篩選結果
        run: |
          # 確保目錄存在
          mkdir -p data/latest
          mkdir -p data/history/$(date +%Y-%m-%d)

          # 檢查檔案是否存在並顯示資訊
          if [ -f "data/latest/隔日衝_篩選結果.csv" ]; then
            echo "=== 篩選結果檔案資訊 ==="
            ls -lh data/latest/隔日衝_篩選結果.csv
            echo "=== 符合條件的股票數量 ==="
            tail -n +2 data/latest/隔日衝_篩選結果.csv | wc -l
            echo "=== 前5筆資料預覽 ==="
            head -6 data/latest/隔日衝_篩選結果.csv
          fi

          # 備份到歷史資料目錄 (帶時間戳記)
          cp data/latest/隔日衝_篩選結果.csv data/history/$(date +%Y-%m-%d)/隔日衝_篩選結果_$(date +%H%M).csv 2>/dev/null || true

          # 顯示目錄內容
          echo "=== 最新資料目錄 ==="
          ls -lh data/latest/

      - name: 提交變更到 GitHub
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add data/
          git diff --staged --quiet || git commit -m "🎯 隔日衝策略 - 篩選結果更新 - $(TZ='Asia/Taipei' date +'%Y-%m-%d %H:%M')"
          git push || echo "沒有變更需要推送"
