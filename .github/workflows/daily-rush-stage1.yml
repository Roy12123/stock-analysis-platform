name: 隔日衝策略 - 階段1 (每日 7:00 台北時間)

on:
  schedule:
    - cron: '0 23 * * *'  # UTC 23:00 = 台北 7:00 (前一天)
  workflow_dispatch:  # 允許手動觸發

permissions:
  contents: write  # 允許推送到 repository

jobs:
  prepare-data:
    runs-on: ubuntu-latest

    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4

      - name: 設置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安裝 Python 依賴
        run: |
          cd python
          pip install -r requirements.txt

      - name: 執行階段1 - 準備歷史資料
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: |
          cd python
          python 隔日衝策略_階段1.py

      - name: 整理資料檔案
        run: |
          # 確保目錄存在
          mkdir -p data/latest
          mkdir -p data/history/$(date +%Y-%m-%d)

          # 檢查檔案是否存在並顯示資訊
          if [ -f "data/latest/隔日衝_歷史資料.csv" ]; then
            echo "=== 歷史資料檔案資訊 ==="
            ls -lh data/latest/隔日衝_歷史資料.csv
            wc -l data/latest/隔日衝_歷史資料.csv
          fi

          # 備份到歷史資料目錄
          cp data/latest/隔日衝_歷史資料.csv data/history/$(date +%Y-%m-%d)/ 2>/dev/null || true

          # 顯示目錄內容
          echo "=== 最新資料目錄 ==="
          ls -lh data/latest/

      - name: 提交變更到 GitHub
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add data/
          git diff --staged --quiet || git commit -m "📊 隔日衝策略 - 歷史資料準備完成 - $(TZ='Asia/Taipei' date +'%Y-%m-%d %H:%M')"
          git push || echo "沒有變更需要推送"
